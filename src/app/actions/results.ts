/**
 * Results Actions (Backwards Compatibility)
 *
 * Re-exports from festivals/results.ts for backwards compatibility.
 * New imports should use @/app/actions/festivals instead.
 */

export { calculateResults, getResults, getResultsData } from "./festivals/results";
